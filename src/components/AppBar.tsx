import '../styles/AppBar.css';

const AppBar = () => {
    return (
        <div className="app-bar">
            <h1 className="app-title">Aplicación de Notas</h1>
            <h2 className="app-subtitle">¡Bienvenido!</h2>
        </div>
    );
};

export default AppBar;
